{
  "ICreateDataElementParams": {
    "why": "Seventeen fields the implementation never sent — the type, the length and every label — left from before adt-clients 19.0.0. A caller who filled them in got a data element without them.",
    "declaration": "export interface ICreateDataElementParams { data_element_name : string ; description ? : string ; package_name : string ; transport_request ? : string ; masterSystem ? : string ; responsible ? : string ; masterLanguage ? : string ; }"
  },
  "IAuthorizationFieldConfig": {
    "why": "Three colSearchhelp fields and an onLock callback nothing invoked.",
    "declaration": "export interface IAuthorizationFieldConfig { authorizationFieldName : string ; packageName ? : string ; description ? : string ; transportRequest ? : string ; masterSystem ? : string ; responsible ? : string ; fieldName ? : string ; rollName ? : string ; checkTable ? : string ; exitFb ? : string ; abapLanguageVersion ? : string ; search ? : string ; objexit ? : string ; domname ? : string ; outputlen ? : string ; convexit ? : string ; orglvlinfo ? : string ; }"
  },
  "IBehaviorDefinitionConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface IBehaviorDefinitionConfig { name : string ; masterLanguage ? : string ; packageName ? : string ; transportRequest ? : string ; description ? : string ; implementationType ? : 'Managed' | 'Unmanaged' | 'Abstract' | 'Projection' ; rootEntity ? : string ; source ? : string ; }"
  },
  "IDataElementConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface IDataElementConfig { source ? : string ; dataElementName : string ; masterLanguage ? : string ; packageName ? : string ; transportRequest ? : string ; description ? : string ; dataType ? : string ; length ? : number ; typeKind ? : 'domain' | 'predefinedAbapType' | 'refToPredefinedAbapType' | 'refToDictionaryType' | 'refToClifType' ; }"
  },
  "IDdlConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface IDdlConfig { ddlName : string ; masterLanguage ? : string ; packageName ? : string ; transportRequest ? : string ; description ? : string ; source ? : string ; }"
  },
  "IDomainConfig": {
    "why": "Three fields nothing sends leave again — decision 31 routes the shape through the document — and the payload field is renamed. One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface IDomainConfig { source ? : string ; domainName : string ; masterLanguage ? : string ; packageName ? : string ; transportRequest ? : string ; description ? : string ; }"
  },
  "IFeatureToggleConfig": {
    "why": "An onLock callback nothing invoked.",
    "declaration": "export interface IFeatureToggleConfig { featureToggleName : string ; packageName ? : string ; description ? : string ; transportRequest ? : string ; masterSystem ? : string ; responsible ? : string ; source ? : IFeatureToggleSource ; }"
  },
  "IFunctionGroupConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface IFunctionGroupConfig { source ? : string ; functionGroupName : string ; masterLanguage ? : string ; packageName ? : string ; transportRequest ? : string ; description ? : string ; masterSystem ? : string ; responsible ? : string ; }"
  },
  "IFunctionIncludeConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface IFunctionIncludeConfig { functionGroupName : string ; includeName : string ; description ? : string ; transportRequest ? : string ; source ? : string ; }"
  },
  "IFunctionModuleConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface IFunctionModuleConfig { functionGroupName : string ; functionModuleName : string ; transportRequest ? : string ; description ? : string ; source ? : string ; masterSystem ? : string ; responsible ? : string ; }"
  },
  "IIncludeConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface IIncludeConfig { includeName : string ; masterLanguage ? : string ; packageName ? : string ; transportRequest ? : string ; description ? : string ; source ? : string ; onLock ? : ( lockHandle : string ) => void ; }"
  },
  "IMetadataExtensionConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface IMetadataExtensionConfig { name : string ; description ? : string ; packageName ? : string ; transportRequest ? : string ; source ? : string ; masterLanguage ? : string ; masterSystem ? : string ; responsible ? : string ; }"
  },
  "IPackageConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface IPackageConfig { source ? : string ; packageName : string ; superPackage ? : string ; description ? : string ; packageType ? : string ; softwareComponent ? : string ; transportLayer ? : string ; transportRequest ? : string ; applicationComponent ? : string ; responsible ? : string ; masterSystem ? : string ; masterLanguage ? : string ; recordChanges ? : boolean ; }"
  },
  "IProgramConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface IProgramConfig { programName : string ; masterLanguage ? : string ; packageName ? : string ; transportRequest ? : string ; description ? : string ; programType ? : string ; application ? : string ; source ? : string ; sessionId ? : string ; }"
  },
  "IServiceBindingConfig": {
    "why": "runTransportCheck, which nothing read.",
    "declaration": "export interface IServiceBindingConfig { bindingName : string ; packageName ? : string ; description ? : string ; serviceDefinitionName ? : string ; serviceName ? : string ; serviceVersion ? : string ; bindingVariant ? : ServiceBindingVariant ; masterLanguage ? : string ; masterSystem ? : string ; responsible ? : string ; desiredPublicationState ? : DesiredPublicationState ; serviceType ? : GeneratedServiceType ; transportRequest ? : string ; }"
  },
  "IStructureConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface IStructureConfig { structureName : string ; masterLanguage ? : string ; packageName ? : string ; transportRequest ? : string ; description ? : string ; source ? : string ; }"
  },
  "ITableTypeConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface ITableTypeConfig { source ? : string ; tableTypeName : string ; masterLanguage ? : string ; packageName ? : string ; transportRequest ? : string ; rowTypeName ? : string ; description ? : string ; }"
  },
  "IAccessControlConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface IAccessControlConfig { accessControlName : string ; masterLanguage ? : string ; packageName ? : string ; transportRequest ? : string ; description ? : string ; source ? : string ; }"
  },
  "IAppendStructureConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface IAppendStructureConfig { appendStructureName : string ; baseObject ? : string ; masterLanguage ? : string ; packageName ? : string ; transportRequest ? : string ; description ? : string ; source ? : string ; }"
  },
  "IBehaviorImplementationConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface IBehaviorImplementationConfig { className : string ; description ? : string ; packageName ? : string ; transportRequest ? : string ; behaviorDefinition : string ; source ? : string ; implementationCode ? : string ; masterSystem ? : string ; responsible ? : string ; }"
  },
  "IClassConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface IClassConfig { className : string ; masterLanguage ? : string ; description ? : string ; packageName ? : string ; transportRequest ? : string ; source ? : string ; testClassCode ? : string ; testClassName ? : string ; localTypesCode ? : string ; definitionsCode ? : string ; macrosCode ? : string ; superclass ? : string ; final ? : boolean ; abstract ? : boolean ; createProtected ? : boolean ; masterSystem ? : string ; responsible ? : string ; classTemplate ? : string ; }"
  },
  "IEnhancementConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface IEnhancementConfig { enhancementName : string ; masterLanguage ? : string ; enhancementType : EnhancementType ; description ? : string ; packageName ? : string ; transportRequest ? : string ; source ? : string ; enhancementSpot ? : string ; badiDefinition ? : string ; }"
  },
  "ILocalDefinitionsConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface ILocalDefinitionsConfig { className : string ; source ? : string ; transportRequest ? : string ; }"
  },
  "ILocalMacrosConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface ILocalMacrosConfig { className : string ; source ? : string ; transportRequest ? : string ; }"
  },
  "ILocalTestClassConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface ILocalTestClassConfig { className : string ; source ? : string ; transportRequest ? : string ; }"
  },
  "ILocalTypesConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface ILocalTypesConfig { className : string ; source ? : string ; transportRequest ? : string ; }"
  },
  "IScalarFunctionConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface IScalarFunctionConfig { scalarFunctionName : string ; masterLanguage ? : string ; packageName ? : string ; transportRequest ? : string ; description ? : string ; source ? : string ; }"
  },
  "IScalarFunctionImplementationConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface IScalarFunctionImplementationConfig { implementationName : string ; scalarFunctionName : string ; engineValue ? : ScalarFunctionEngine ; masterLanguage ? : string ; packageName ? : string ; transportRequest ? : string ; description ? : string ; source ? : string ; }"
  },
  "IServiceDefinitionConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface IServiceDefinitionConfig { serviceDefinitionName : string ; masterLanguage ? : string ; packageName ? : string ; transportRequest ? : string ; description ? : string ; source ? : string ; }"
  },
  "ITableConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface ITableConfig { tableName : string ; masterLanguage ? : string ; packageName ? : string ; transportRequest ? : string ; source ? : string ; description ? : string ; }"
  },
  "ITransformationConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface ITransformationConfig { transformationName : string ; masterLanguage ? : string ; transformationType : TransformationType ; packageName ? : string ; transportRequest ? : string ; description ? : string ; source ? : string ; }"
  },
  "ITransportConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface ITransportConfig { source ? : string ; description : string ; transportType ? : 'workbench' | 'customizing' ; targetSystem ? : string ; owner ? : string ; transportNumber ? : string ; }"
  },
  "IUnitTestConfig": {
    "why": "One name for what a write sends. The payload field carried eleven different names across 35 fields — `sourceCode` in 15 configs, `document` in 6, `ddlCode` and `ddlSource` for the same thing in neighbouring files, `testClassCode` beside `testClassSource`. It is `source` everywhere now: ABAP text for the types that have one, XML for the types that are their own document, and what those XML look like is documented rather than typed.",
    "declaration": "export interface IUnitTestConfig { className : string ; source ? : string ; packageName ? : string ; description ? : string ; classTemplate ? : string ; transportRequest ? : string ; }"
  }
}
