# Symbols the facade exports that 44.0.0 did not.
#
# `surface-44.0.0.txt` is the contract the split had to preserve, and the check
# compares against it in BOTH directions: nothing from 44.0.0 may disappear,
# and nothing may appear. The second half is what catches a symbol leaking out
# of a package by accident — worth keeping — but it also stops the facade ever
# gaining anything on purpose.
#
# So an addition is declared here, one `<name> <kind>` per line, and becomes a
# deliberate act rather than a silent one. A symbol added to a package and not
# written here still fails the check.

IAbapObjectEntry type
IAdtTransportObjectActions type
